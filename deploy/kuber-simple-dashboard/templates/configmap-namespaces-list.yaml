apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kuber-simple-dashboard.fullname" . }}-ns-list
  namespace: {{ .Values.namespace | quote }}
data:
  namespaces.json: |
    {
      "optional_name": {{ .Values.optional_name | quote }},
      "namespaces": [
        {{- range .Values.list_namespace }}
        "{{ . }}",
        {{- end }}
       ]
    }
